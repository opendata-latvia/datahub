.inputs
  = f.input :title, :autofocus => true, :input_html => {:class => "span4"}
  = f.input :slug, :input_html => {:class => "span4", :"data-auto-sluggify" => "#topic_title"}
  = f.input :commentable
.inputs
  = f.input :description, :as => :markdown, :input_html => {:rows => 5, :class => "span10"}
  
  
.controls
  = f.button :submit, t("actions.update"), :class => 'btn-primary'
  &nbsp;
  = t "nav.or"
  &nbsp;
  = link_to t("nav.back"), :back
