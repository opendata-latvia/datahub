.span12
  %h2= t "topics.edit_topic_in_forum", :forum => @forum.title
  = simple_form_for(@topic, :url => forum_topic_path(@forum.slug, @topic.slug.blank? ? @topic.slug_was : @topic.slug), :html => {:class => 'project-form well form-horizontal'}) do |f|
    = render 'form', :f => f
